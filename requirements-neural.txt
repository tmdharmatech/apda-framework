# Dependências neurais do APDA Framework (Privacy Filter)
#
# Estas dependências são necessárias APENAS para o workflow de anonimização
# com o Privacy Filter neural (etapa privacy-filter).
#
# Instale somente se for usar workflows com anonimização neural:
#   pip install -r requirements-neural.txt
#
# Esta máquina não possui GPU NVIDIA. Usamos o índice CPU-only da PyTorch para
# evitar downloads de pacotes CUDA/NVIDIA.

--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

torch==2.11.0+cpu      # backend de tensor exigido pelo transformers pipeline (CPU-only)
transformers>=4.40.0   # pipeline de NLP (NER para anonimização neural)
